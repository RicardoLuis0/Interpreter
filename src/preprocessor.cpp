#include "preprocessor.h"

std::string preprocess_file(std::string){
    std::string source;
    //TODO read file
    return preprocess(source);
}

std::string preprocess(std::string &){
    //TODO write own preprocessor
    return "";
}
