#include "preprocessor.h"

std::string preprocess_file(std::string filename){
    std::string source;
    //TODO read file
    return preprocess(source);
}

std::string preprocess(std::string &source){
    //TODO write own preprocessor
    return "";
}
